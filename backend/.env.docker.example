# Docker Environment Variables
# Copy this to .env.docker and update with your values
# Load with: docker-compose --env-file .env.docker up

# CORS - Frontend domain (required)
ALLOWED_ORIGINS=https://fredrikmeyer.github.io

# Production mode (should be true for Docker deployment)
# PRODUCTION=true  # Set in docker-compose.yml by default

# Data file location (handled by volume mount)
# DATA_FILE=/app/data/storage.json  # Set in docker-compose.yml by default
