# Backend Configuration Example
# Copy this file to .env and update with your values

# CORS Configuration
# Allowed origins for cross-origin requests (comma-separated for multiple origins)
# Use "*" to allow all origins (not recommended for production)
# Examples:
#   Single origin: ALLOWED_ORIGINS=https://username.github.io
#   Multiple origins: ALLOWED_ORIGINS=https://username.github.io,https://example.com
#   Development (all origins): ALLOWED_ORIGINS=*
ALLOWED_ORIGINS=*

# Data Storage
# Path to JSON file for storing data
# Default: backend/data/storage.json
# ASTHMA_DATA_FILE=backend/data/storage.json

# Production Mode
# Set to "true" when running in production
# PRODUCTION=false

# Rate Limiting (to be implemented in Phase 2.3)
# RATE_LIMIT_PER_MINUTE=60
